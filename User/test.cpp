#include "user.hpp"
#include <iostream>
#include <string>

using namespace std;

int main() {

    User u = User("Graham", "0F 45 72 40");

    if (u.getUsername = "Graham") {
        cout << "Username works" << endl;
    }

    if (u.getUID = "Graham") {
        cout << "Username works" << endl;
    }

    if (u.getUsername = "Graham") {
        cout << "Username works" << endl;
    }

    if (u.getUsername = "Graham") {
        cout << "Username works" << endl;
    }

    if (u.getUsername = "Graham") {
        cout << "Username works" << endl;
    }

    if (u.getUsername = "Graham") {
        cout << "Username works" << endl;
    }

    if (u.getUsername = "Graham") {
        cout << "Username works" << endl;
    }

    if (u.getUsername = "Graham") {
        cout << "Username works" << endl;
    }

    char chrs[2];
    if (u.intToChr(chrs, 255)) {
        cout << chrs[1] << chrs[0] << endl;
    }

    return 0;
}